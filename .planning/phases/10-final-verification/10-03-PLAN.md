---
phase: 10-final-verification
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Production build succeeds with zero errors"
    - "Full test suite (426+ tests) passes green"
    - "Security headers present on production (HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, CSP)"
    - "Rate limiting returns 429 when limits exceeded"
    - "Admin routes return 404 for non-admin users"
    - "Map displays side-by-side layout with working product links"
  artifacts: []
  key_links:
    - from: "production deployment"
      to: "security headers"
      via: "HTTP response headers"
      pattern: "strict-transport-security|x-frame-options|content-security-policy"
---

<objective>
Run automated verification checks against the codebase and production deployment: build, tests, security headers, rate limiting, and visual confirmation of the map fix.

Purpose: Automated checks catch regressions and verify production configuration before manual flow testing begins.
Output: Verified production build, green test suite, confirmed security posture.
</objective>

<execution_context>
@/Users/tasmanstar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tasmanstar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-final-verification/10-01-SUMMARY.md
@.planning/phases/10-final-verification/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated verification suite</name>
  <files></files>
  <action>
Run the following automated checks in sequence. If any check fails, STOP and fix the issue before continuing.

1. **TypeScript check:** `npx tsc --noEmit` -- must complete with zero errors

2. **Full test suite:** `npx vitest run` -- must show 0 failures (426+ tests)

3. **Production build:** `npm run build` -- must complete without errors. Note any warnings but do not fail on warnings alone.

4. **Security headers on production:** Run:
   ```bash
   curl -sI https://tasman-admin.vercel.app/ | grep -iE "strict-transport|x-frame|x-content-type|referrer-policy|permissions-policy|content-security"
   ```
   Verify ALL of these headers are present:
   - Strict-Transport-Security
   - X-Frame-Options
   - X-Content-Type-Options
   - Referrer-Policy
   - Permissions-Policy
   - Content-Security-Policy (or Content-Security-Policy-Report-Only)

5. **Admin access control:** Run:
   ```bash
   curl -sI https://tasman-admin.vercel.app/admin
   ```
   Verify: non-authenticated request does NOT return 200 with admin page content. Should return 404 or redirect to login.

6. **Rate limit endpoint check (informational):** Verify rate limit module exists and is configured:
   ```bash
   grep -n "globalLimiter\|authLimiter\|apiLimiter" src/lib/rate-limit.ts
   grep -n "upstashRateLimit" src/middleware.ts | head -5
   ```

Record all results. If any check fails, fix the issue inline before proceeding.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>TypeScript clean, all tests green, production build succeeds, security headers verified on production.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of map layout and product links</name>
  <files></files>
  <action>
Present the map changes to the user for visual verification. Start the dev server if not running (`npm run dev`). The user needs to verify the map layout fix from Plan 02 works correctly on both desktop and mobile viewports, and that species cards link to the correct product pages.
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting human verification"</automated>
  </verify>
  <done>User approves map layout: side-by-side on desktop, stacked on mobile, species link to correct products, no removed species visible.</done>
  <what-built>Automated verification suite completed. Map layout has been restructured (Plan 02), tests fixed (Plan 01), build verified, security headers checked.</what-built>
  <how-to-verify>
    1. Visit https://tasman-admin.vercel.app/ (or run `npm run dev` locally)
    2. Scroll to the "Explore Our Waters" map section
    3. On desktop (>1024px): verify the info panel appears to the RIGHT of the map (not below)
    4. Click a region (e.g., Queensland) -- verify species cards appear in the side panel
    5. Click a species card (e.g., Barramundi) -- verify it navigates to /product/barramundi-fillets-skin-on
    6. Verify NO species without products appear (no Pearl Meat, Dhufish, Gummy Shark, Blue Swimmer Crab, Abalone, Hoki)
    7. On mobile viewport (resize to <768px): verify info panel appears BELOW the map
    8. Verify the 3D isometric map effect still works on desktop
    9. Check both light and dark themes
  </how-to-verify>
  <resume-signal>Type "approved" if map layout and links work correctly, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
All automated checks pass. Map layout visually confirmed by user.
</verification>

<success_criteria>
- TypeScript, tests, and build all green
- Security headers confirmed on production
- Map layout visually approved by user (side-by-side desktop, stacked mobile)
- Species cards link to correct product pages
</success_criteria>

<output>
After completion, create `.planning/phases/10-final-verification/10-03-SUMMARY.md`
</output>
