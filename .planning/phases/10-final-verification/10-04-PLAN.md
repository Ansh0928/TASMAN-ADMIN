---
phase: 10-final-verification
plan: 04
type: execute
wave: 3
depends_on: ["10-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "A new customer can browse, add to cart, complete checkout, and receive order confirmation"
    - "A wholesale applicant can apply, get approved, log in to wholesale portal, view prices, and place an order"
    - "An admin can log in, manage products/orders/customers, view analytics, and export data"
    - "All flows work on mobile without layout issues"
    - "Security headers, rate limiting, and role-based access are verified in production"
  artifacts: []
  key_links: []
---

<objective>
Systematically verify all production user flows: customer checkout, wholesale lifecycle, admin panel management, auth flows, mobile responsiveness, and SEO/accessibility. Fix any issues found immediately.

Purpose: This is the final production readiness gate. Every user flow must work perfectly before launch.
Output: All flows verified or issues fixed inline. Production ready.
</objective>

<execution_context>
@/Users/tasmanstar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tasmanstar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-final-verification/10-CONTEXT.md
@.planning/phases/10-final-verification/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute comprehensive production verification checklist</name>
  <files></files>
  <action>
Run through each verification category systematically. For each test, record PASS/FAIL. If anything FAILS, fix it immediately before continuing (per user's "100% pass bar" requirement).

**CATEGORY 1: Auth Flows**
Test on production (https://tasman-admin.vercel.app/) or locally (npm run dev):
- [ ] Customer registration: create new account, verify password complexity enforced (try weak password first)
- [ ] Customer login: log in with new account, verify session
- [ ] Session persistence: close tab, reopen, verify still authenticated
- [ ] Customer logout: verify redirect and session cleared
- [ ] Admin login: log in at /admin/login with techsupport@tasmanstarseafood.com / Admin1234!
- [ ] Admin logout: verify redirect and session cleared
- [ ] Unauthorized access: visit /admin/products as customer -- verify 404 (not redirect or admin content)
- [ ] Unauthenticated access: visit /account while logged out -- verify redirect to login

**CATEGORY 2: Customer Checkout Flow**
- [ ] Browse /our-products -- products display correctly
- [ ] Product detail: click a product, verify /product/[slug] loads with images, price, description
- [ ] Add to cart: add product, verify cart updates
- [ ] Cart sidebar: open cart, verify items, quantities, prices
- [ ] Checkout page: navigate to /checkout, verify delivery fee shown before payment
- [ ] Note: Do NOT complete actual Stripe payment with live keys. Verify the checkout page renders correctly, form validates, and Stripe elements load.

**CATEGORY 3: Wholesale Flow**
- [ ] Visit /wholesale/apply -- form loads, submit application
- [ ] Admin: log in, go to /admin/wholesale, verify application appears
- [ ] Admin: approve the application
- [ ] Wholesale login: log in with wholesale credentials at /wholesale/login
- [ ] Wholesale prices: visit /wholesale/prices, verify prices display
- [ ] Wholesale order: place an order, verify it appears in /admin/wholesale-orders
- [ ] Access control: log in as regular customer, visit /wholesale/prices -- verify blocked

**CATEGORY 4: Admin Panel**
- [ ] Dashboard: /admin -- verify analytics (revenue, products, trends, customer counts)
- [ ] Products: /admin/products -- list, create new (with image upload), edit, verify on storefront
- [ ] Orders: /admin/orders -- list, view details, change status (pending -> confirmed -> shipped)
- [ ] CSV export: download orders CSV, verify columns and data
- [ ] Customers: /admin/customers -- list, search, view details
- [ ] Wholesale orders: /admin/wholesale-orders -- list, update status
- [ ] Categories: /admin/categories -- manage product categories

**CATEGORY 5: Product Navigation**
- [ ] /our-products -- all products display, category filtering works
- [ ] /deals -- deals page loads with deal products
- [ ] Product detail -- related products section shows correctly
- [ ] Search functionality (if exists) -- returns relevant results

**CATEGORY 6: SEO and Performance (automated)**
Run Lighthouse on key pages (if npx lighthouse available):
```bash
npx lighthouse https://tasman-admin.vercel.app/ --output=json --chrome-flags="--headless --no-sandbox" 2>/dev/null | node -e "const d=require('fs').readFileSync('/dev/stdin','utf8');const j=JSON.parse(d);Object.entries(j.categories).forEach(([k,v])=>console.log(k+': '+Math.round(v.score*100)))"
```
Targets: Performance 80+, Accessibility 90+, SEO 90+, Best Practices 90+

Also check:
- [ ] sitemap.xml accessible at /sitemap.xml
- [ ] robots.txt accessible at /robots.txt
- [ ] Meta tags present on key pages (title, description, og:image)
- [ ] Custom 404 page at /nonexistent-page

**CATEGORY 7: Error Handling**
- [ ] 404 page: visit /this-page-does-not-exist -- verify custom branded 404
- [ ] API error: curl an invalid API endpoint, verify proper error JSON response

For any failures: fix the code immediately, run tests to confirm no regressions, and continue verification.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>All verification categories have been checked. Any failures found were fixed inline. Test suite still green after all fixes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of mobile, browser compatibility, and accessibility</name>
  <files></files>
  <action>
Present verification results to user and request manual testing on real devices and browsers. The user needs to verify mobile responsiveness, cross-browser compatibility, accessibility, and account management flows that require visual/interactive confirmation.
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting human verification of mobile and browser compatibility"</automated>
  </verify>
  <done>User confirms production readiness: mobile layouts work, browsers compatible, accessibility acceptable.</done>
  <what-built>Complete production verification checklist executed. All automated checks passed. Any issues found during verification were fixed.</what-built>
  <how-to-verify>
    The following flows need human confirmation on a real device or browser:

    **Mobile verification (use real phone or Chrome DevTools at 375px width):**
    1. Homepage -- hero, map, product grid display correctly
    2. Product detail page -- images, add to cart button, touch targets 44px+
    3. Cart -- full-screen overlay on mobile, quantity controls work
    4. Navigation -- hamburger menu works, all links accessible
    5. Admin panel -- tables scroll horizontally, sidebar collapses
    6. Wholesale portal -- apply form, prices page usable on mobile
    7. Check both light and dark themes on mobile

    **Browser compatibility (spot check on each):**
    - Chrome desktop + mobile
    - Safari desktop + iOS
    - Firefox
    - Edge

    **Accessibility spot check:**
    - Tab through main navigation -- focus indicators visible
    - Check color contrast on key text (should be 4.5:1+)
    - Screen reader: key pages have proper heading structure

    **Account management (if time permits):**
    - Order history: logged-in customer views past orders
    - Address management: add, edit, delete delivery addresses
    - Profile editing: update name, email
  </how-to-verify>
  <resume-signal>Type "approved" if production is ready for launch, or list specific issues that need fixing</resume-signal>
</task>

</tasks>

<verification>
All verification categories pass. Human confirms mobile, browser compatibility, and accessibility.
</verification>

<success_criteria>
- Every auth flow works (register, login, logout, unauthorized access returns 404)
- Customer can browse and reach checkout (Stripe elements load)
- Wholesale lifecycle complete (apply -> approve -> login -> prices -> order)
- Admin can manage all entities (products, orders, customers, wholesale)
- Mobile layouts verified on real device or DevTools
- SEO basics confirmed (sitemap, robots, meta tags, custom 404)
- Security posture confirmed (headers, rate limiting, access control)
- Full test suite green after all fixes
</success_criteria>

<output>
After completion, create `.planning/phases/10-final-verification/10-04-SUMMARY.md`
</output>
